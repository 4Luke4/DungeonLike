<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
        Base theme for the host window.

        The window is edge to edge and has no action bar: the engine renders the
        entire interface, and a system-drawn bar would overlap it.
    -->
    <style name="Theme.DungeonLike" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/surface_background</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:navigationBarColor" tools:targetApi="35">@android:color/transparent</item>
        <item name="android:statusBarColor" tools:targetApi="35">@android:color/transparent</item>
    </style>

    <!--
        Applied while the system splash screen is showing. The engine takes a
        moment to initialise; without a splash theme the player sees an empty
        window and reads it as a hang. Theme.DungeonLike is installed as the
        post-splash theme by the activity.
    -->
    <style name="Theme.DungeonLike.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DungeonLike</item>
    </style>

</resources>
